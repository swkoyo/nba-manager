import { Text } from '@mantine/core';

export default function Home() {
    return (
        <Text size='xl'>
            Welcome to the NBA Database Project! Use the buttons on the left to
            navigate through the site.
        </Text>
    );
}
